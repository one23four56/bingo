{
  "dependencies": {
    "esbuild": "^0.18.10",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "build": "esbuild src/home.ts src/project.ts --bundle --outdir=static --format=iife --sourcemap --watch"
  }
}
