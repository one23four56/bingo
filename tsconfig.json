{
    "compilerOptions": {
        "target": "ES6",
        "module": "ES6",
        "lib": [
            // if ts is throwing bs errors, try adding shit here lol
            "ES2016", 
            "DOM", 
            "DOM.Iterable",
            "DOM.AsyncIterable",
        ]
    }
}